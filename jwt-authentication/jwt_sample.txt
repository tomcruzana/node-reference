const jwt = require("jsonwebtoken");

const func = async ()=>{
    const token = jwt.sign({_id: "abc"}, "thisismyass", { expiresIn: "7 seconds"})
    console.log(token)

    const data = jwt.verify(token, "thisismyass")
    console.log(data)
};

func();
